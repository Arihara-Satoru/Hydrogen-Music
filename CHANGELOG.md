# 变更日志

本文件记录 Hydrogen Music 的重要用户可见变更。

## 未发布

### 新增

- 以 Endfield 风格重新设计桌面歌词窗口，采用浅色工程轨道、炭黑控制坞、信号黄状态标记、分段封面场和大型歌词行号。
- 新增常驻的锁定与设置入口；右键仍可打开完整控制面板。
- 桌面歌词设置新增精简模式，仅保留当前歌词和下一句歌词；开启后窗口自动收缩并允许调整至 `360 × 180`，退出时恢复原尺寸。
- 普通且未锁定模式下，悬停或聚焦封面即可控制上一首、播放暂停和下一首，不改变原有窗口布局。
- 新增开发态桌面歌词预览数据入口，用于在不写入播放器状态或本地存储的情况下检查典型播放界面。

### 改进

- 当前歌词的扫描进度现在由真实播放时间和歌词时间戳驱动，并随暂停、切歌和歌词行切换同步更新。
- 桌面歌词现在显示当前封面、歌曲来源、已播放时间、总时长以及真实的歌曲和歌词行进度。
- 未锁定桌面歌词时，底部播放进度条支持点击、拖动和键盘调整播放位置；锁定后自动恢复为只读状态。
- 歌词来源选择、字号调整、位置锁定和关闭操作改为语义化控件，支持键盘焦点与 `Esc` 关闭设置面板。
- 重新编排宽屏、窄窗、竖向窗口和短宽窗口布局，并提供减少动态效果及低刷新设备的降级方案。
- 默认窗口尺寸调整为 `760 × 360`，可调整范围扩展为 `520 × 280` 至 `1200 × 640`。

### 修复

- 修复深色模式下桌面歌词仍使用浅色表面、全局文字色覆盖组件配色，导致标题、下一句和状态信息难以辨认的问题。
- 修复将播放秒数误当百分比计算当前时间的问题。
- 移除与实际播放无关的固定十秒歌词扫描动画。
- 修复点击歌词内容或控制按钮时可能误触发窗口拖动的问题；窗口现在仅能通过工程轨道和标题区域拖动。
- 修复歌曲切换后手动选择的歌词来源不再可用时显示空白的问题。
- 清理桌面歌词窗口销毁后的 IPC 监听，避免重复挂载。
